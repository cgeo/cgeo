<?xml version="1.0" encoding="utf-8"?>
<cgeo.geocaching.ui.SearchCardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="wrap_content"
    android:layout_width="0dp"
    android:layout_columnWeight="1"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardView"
    android:layout_margin="6dp"
    app:cardElevation="1dp"
    app:cardCornerRadius="4dp"
    app:strokeColor="@color/colorSeparator"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:gravity="center">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            app:tint="@color/colorText"
            android:src="@drawable/sc_search" />

        <TextView
            android:id="@+id/text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/textSize_buttonsDouble"
            android:textColor="@color/colorText"
            android:textAlignment="center"
            android:layout_marginTop="8dp" />

    </LinearLayout>

</cgeo.geocaching.ui.SearchCardView>
