<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="AlwaysShowAction">

    <!-- All items should use showAsAction="always",
    as we don't want to have an overflow menu.
    For less important items we have a dedicated MoreActivity. -->

    <item
        android:id="@+id/menu_gosearch"
        android:icon="@drawable/ic_menu_search"
        android:title="@string/search_bar_hint"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:iconifiedByDefault="true"
        app:showAsAction="collapseActionView|always" />

</menu>
