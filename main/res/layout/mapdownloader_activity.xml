<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    tools:context=".settings.MapDownloadSelectorActivity" >

    <Button
        android:id="@+id/check_for_updates"
        style="@style/button_full"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:text="@string/downloadmap_checkforupdates_button"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/mapdownloader_list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_margin="0dip"
        android:background="?background_color"
        android:cacheColorHint="?background_color"
        android:clipToPadding="false"
        android:dividerHeight="1dip"
        android:padding="0dip"
        android:scrollbarStyle="outsideOverlay"
        tools:listitem="@layout/mapdownloader_item" />

</LinearLayout>
