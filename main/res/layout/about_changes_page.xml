<?xml version="1.0" encoding="UTF-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:padding="4dip"
    tools:context=".AboutActivity$ChangeLogViewCreator">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <!-- ToDo: remove celebration image and corresponding resource as soon as our party is over ;-) -->
        <ImageView
            android:id="@+id/about_version_icon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="10dip"
            android:gravity="center"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            android:src="@drawable/cgeo_10th_birthday" />
        <!-- end of ToDo -->

        <TextView
            android:id="@+id/changelog_master"
            style="@style/about_changes"
            android:paddingBottom="20dip" />

        <TextView
            android:id="@+id/changelog_release"
            style="@style/about_changes" />

        <Button
            android:id="@+id/changelog_github"
            style="@style/button_full"
            android:text="@string/changelog_github" />
    </LinearLayout>

</ScrollView>
