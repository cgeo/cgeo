/**
 * Connector capabilities describe self-contained, scoped functionality of a connector. By adding more capability
 * implementations to a certain connector, it supports more use cases in the app, since the user interface always
 * queries for certain capabilities to enable certain UI elements.
 */
package cgeo.geocaching.connector.capability;
