<?xml version="1.0" encoding="UTF-8"?>
<resources>

    <declare-styleable name="cgCompassMini">
        <attr name="skin" format="integer" />
    </declare-styleable>

    <!-- drawables -->
    <attr name="button" format="integer" />

    <!-- attributes for custom made preferences -->
    <attr name="text" format="string" />
    <attr name="popupTitle" format="string" />
    <attr name="url" format="string" />
    <attr name="urlButton" format="string" />
    <attr name="connector" format="string" />

    <!-- attributes for ButtonPreference -->
    <declare-styleable name="ButtonPreference">
        <attr name="src" format="integer" />
    </declare-styleable>

    <!-- attributes for ColorpickerPreference -->
    <declare-styleable name="ColorpickerPreference">
        <attr name="showOpaquenessSlider" format="boolean" />
        <attr name="defaultColor" format="color" />
        <attr name="lineWidthKey" format="string" />
        <attr name="defaultLineWidth" format="integer" />
    </declare-styleable>

    <!-- attributes for SeekbarPreference -->
    <declare-styleable name="SeekbarPreference">
        <attr name="min" format="integer" />
        <attr name="max" format="integer" />
        <attr name="minValueDescription" format="string" />
        <attr name="maxValueDescription" format="string" />
        <attr name="stepSize" format="integer" />
        <attr name="label" format="string" />
        <attr name="hasDecimals" format="boolean" />
        <attr name="logScaling" format="boolean" />
        <attr name="ui" format="string" />
        <attr name="highRes" format="boolean" /> <!-- only used in ProximityPreference -->
    </declare-styleable>

    <!-- button style for home screen's quick launch buttons -->
    <attr name="quickLaunchButtonStyle" format="reference"/>

    <!-- custom attributes for FormulaEditView -->
    <declare-styleable name="FormulaEditText">
        <attr name="hint" format="string" />
    </declare-styleable>

    <!-- others -->
    <attr name="compass" format="integer" />
    <attr name="progressSpinnerLarge" format="integer" />


    <declare-styleable name="SectionHeader">
        <attr name="android:text"/>
        <attr name="android:id"/>
        <attr name="dividerAbove" format="boolean" />
    </declare-styleable>

</resources>
