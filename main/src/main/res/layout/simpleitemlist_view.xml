<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/simpleitemlist_fragment"

    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Filter -->
    <com.google.android.material.textfield.TextInputLayout android:id="@+id/listFilterContainer" style="@style/textinput_edittext_singleline">
        <EditText
            android:id="@+id/listFilter"
            style="@style/textinput_embedded_singleline"
            android:inputType="text"
            android:hint="@string/search_filter" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Item List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:footerDividersEnabled="false"
        android:headerDividersEnabled="false"
        android:nestedScrollingEnabled="false"
        android:orientation="vertical"
        android:padding="4dip"
        android:scrollbars="none"/>

</merge>
