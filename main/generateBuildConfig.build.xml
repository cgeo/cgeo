<?xml version="1.0" encoding="UTF-8"?>
<!-- ======================================================================

     generateBuildConfigForEclipse
     generates a build config file for eclipse, which doesn't even have the notion of build configs (like gradle)

     ====================================================================== -->
<project name="generateBuildConfigForEclipse" default="default">
    <description>
            generates a build config file for eclipse, which doesn't even have the notion of build configs (like gradle)
    </description>

    <!-- =================================
          target: default
         ================================= -->
    <target name="default" description="generates a build config file for eclipse, which doesn't even have the notion of build configs (like gradle)">
        <copy file="templates\BuildConfig.java" todir="gen\cgeo\geocaching" overwrite="true"/>
    </target>

</project>
